{
  "name": "punch-in-system",
  "private": true,
  "version": "1.0.0",
  "description": "Geofenced workspace attendance PWA — deployment tooling",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:api": "bash scripts/deploy-firebase.sh",
    "tunnel": "bash scripts/tunnel-api.sh",
    "start": "bash scripts/start-all.sh",
    "stop": "bash scripts/stop-all.sh",
    "docker:up": "docker compose up -d --build && docker compose ps",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f api",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy preview",
    "firebase": "firebase",
    "login": "firebase login",
    "whoami": "firebase login:list"
  },
  "devDependencies": {
    "firebase-tools": "^13.35.1"
  }
}
